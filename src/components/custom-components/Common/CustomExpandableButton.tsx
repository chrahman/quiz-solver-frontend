import { ButtonGroup } from '@heroui/react';

const CustomExpandableButton = ({ mainButton }) => {
  return <ButtonGroup variant="flat">{mainButton}</ButtonGroup>;
};

export default CustomExpandableButton;
